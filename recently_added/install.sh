#!/bin/bash
# Recently Added plugin installer.
#
# Volumio's plugin manager handles npm install from package.json and
# folder management itself when 'volumio plugin install' runs, so this
# script just needs to print 'plugininstallend' to signal completion.
echo "=== Recently Added Plugin Installer ==="
echo "plugininstallend"
